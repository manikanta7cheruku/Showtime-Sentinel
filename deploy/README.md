# Deployment Files

This folder contains files used when deploying Showtime Sentinel to a production server.

## Files

- `showtime-sentinel.service` — systemd service definition for Ubuntu/Linux servers

## Usage

These files are copied to a Linux server during deployment. They are NOT used during local Windows development.

See `docs/DEPLOYMENT.md` for the full deployment guide.